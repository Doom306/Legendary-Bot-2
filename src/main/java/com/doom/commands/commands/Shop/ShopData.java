package com.doom.commands.commands.Shop;

import net.dv8tion.jda.api.entities.User;

import java.util.HashMap;

public class ShopData {
    public static HashMap<User, Integer> luckyCharm = new HashMap<>();
    public static HashMap<User, Integer> gold = new HashMap<>();
    public static HashMap<User, Integer> silver = new HashMap<>();
    public static HashMap<User, Integer> fish = new HashMap<>();
    public static HashMap<User, Integer> goldenEgg = new HashMap<>();
    public static HashMap<User, Integer> legendaryDragon = new HashMap<>();
}
